# 樹洞 AI 切片 C：三層 rate limit + 揭露精修 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 三層防護避免 token 爆量：localStorage 軟限制 1 封/天（UX 提醒）、Vercel KV IP 硬限制 5 次/天/IP（反濫用）、env var 月預算 cutoff（兜底）。前端揭露文案位置精修。

**Architecture:** 限制都疊在 `/api/respond` 之外與內部：前端在 submit() 前查 localStorage，達標就阻擋；後端 handler 第一步檢查 IP 計數（Vercel KV），達標回 429；ENV 設定 cutoff 日期過了就直接 fallback 不打 Gemini。前端揭露文案保留在信件下方但稍微調整視覺權重。

**Tech Stack:** Vercel KV (Redis-backed) · `@vercel/kv` SDK · 既有 vitest mock 模式 · localStorage Web API

---

## 檔案結構

| 路徑 | 動作 | 責任 |
|---|---|---|
| `apps/web-pwa/src/lib/daily-limit.ts` | 新增 | localStorage 軟限制：`hasUsedLetterToday()`、`markLetterSent()` |
| `apps/web-pwa/src/lib/__tests__/daily-limit.test.ts` | 新增 | localStorage mock 測試 4 cases |
| `apps/web-pwa/src/App.tsx` | 修改 | submit() 前查 daily-limit、限制 UI 訊息、寫信成功後 markLetterSent |
| `apps/web-pwa/src/styles.css` | 修改 | 新增 .daily-limit-notice 樣式 |
| `api/_lib/rate-limit.ts` | 新增 | IP rate limit：`checkAndIncrementIp(ip): { allowed, count, limit }` |
| `api/_lib/__tests__/rate-limit.test.ts` | 新增 | Vercel KV mock 測試 4 cases |
| `api/_lib/budget.ts` | 新增 | 月預算 cutoff：`isBudgetExhausted(): boolean` |
| `api/_lib/__tests__/budget.test.ts` | 新增 | 環境變數測試 3 cases |
| `api/respond.ts` | 修改 | 加 IP rate limit check、加 budget check |
| `package.json` (root) | 修改 | 加 `@vercel/kv` 依賴 |
| `.env.example` | 修改 | 加 `KV_REST_API_URL` / `KV_REST_API_TOKEN` / `BUDGET_HARD_CUTOFF_DATE` |

**為何 daily-limit 與 rate-limit 分檔**：前者跑前端（localStorage）、後者跑後端（Vercel KV）。技術 stack 不同、測試 mock 模式不同、各自獨立。

---

## Vercel KV 取得 + 設定步驟

### Step 0.1：在 Vercel dashboard 建立 KV store

- [ ] 開 https://vercel.com/dashboard
- [ ] 點專案 `pharmacist-tree-hollow`
- [ ] 上方 tab 點 **Storage**
- [ ] 點 **Create Database**
- [ ] 選 **KV (Redis)** → Continue
- [ ] Name 填 `pharmacist-tree-hollow-rate-limit`
- [ ] Region 選距離最近的（亞洲：Tokyo 或 Singapore）
- [ ] 點 **Create**

### Step 0.2：把 KV 連到專案

- [ ] 建好後會自動跳到 KV detail 頁
- [ ] 點 **Connect to Project**（如果沒自動連）
- [ ] 選 `pharmacist-tree-hollow`、所有 environments
- [ ] Vercel 會自動把 `KV_REST_API_URL`、`KV_REST_API_TOKEN`、`KV_REST_API_READ_ONLY_TOKEN`、`KV_URL` 加到專案 env vars

### Step 0.3：pull env 到本地測試

```bash
cd /Users/lcc/Desktop/PGY許願池/藥師樹洞
vercel env pull .env.local
```

預期：`.env.local` 增加 4 個 KV_* 變數。**不要 commit**（已被 .gitignore）。

### Step 0.4：驗證 KV 連通

不在 task 範圍、直接用 Step 0.5 的 Task 4 驗證。

---

## Task 1：daily-limit（localStorage 軟限制 / TDD）

**Files:**
- Create: `apps/web-pwa/src/lib/daily-limit.ts`
- Create: `apps/web-pwa/src/lib/__tests__/daily-limit.test.ts`

- [ ] **Step 1.1：寫失敗測試**

`apps/web-pwa/src/lib/__tests__/daily-limit.test.ts`：

```ts
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import { hasUsedLetterToday, markLetterSent } from "../daily-limit";

const KEY = "pharmacist-tree-hollow:lastLetterDate";

describe("daily-limit", () => {
  beforeEach(() => {
    localStorage.clear();
  });

  afterEach(() => {
    localStorage.clear();
  });

  it("第一次進來：hasUsedLetterToday 回 false", () => {
    expect(hasUsedLetterToday()).toBe(false);
  });

  it("markLetterSent 寫入今天日期、hasUsedLetterToday 回 true", () => {
    markLetterSent();
    expect(hasUsedLetterToday()).toBe(true);
  });

  it("如果 localStorage 存的是昨天日期、hasUsedLetterToday 回 false", () => {
    const yesterday = new Date();
    yesterday.setDate(yesterday.getDate() - 1);
    localStorage.setItem(KEY, yesterday.toISOString().slice(0, 10));
    expect(hasUsedLetterToday()).toBe(false);
  });

  it("如果 localStorage 不可用、silently allow（回 false）", () => {
    const originalLocalStorage = global.localStorage;
    // @ts-expect-error -- 暫時破壞 localStorage
    delete global.localStorage;
    expect(hasUsedLetterToday()).toBe(false);
    expect(() => markLetterSent()).not.toThrow();
    global.localStorage = originalLocalStorage;
  });
});
```

- [ ] **Step 1.2：跑測試確認失敗**

```bash
cd /Users/lcc/Desktop/PGY許願池/藥師樹洞
npm test -- daily-limit.test
```

預期：import 失敗。

- [ ] **Step 1.3：建立 `apps/web-pwa/src/lib/daily-limit.ts`**

```ts
const KEY = "pharmacist-tree-hollow:lastLetterDate";

function today(): string {
  return new Date().toISOString().slice(0, 10);
}

export function hasUsedLetterToday(): boolean {
  try {
    if (typeof localStorage === "undefined") return false;
    return localStorage.getItem(KEY) === today();
  } catch {
    return false;
  }
}

export function markLetterSent(): void {
  try {
    if (typeof localStorage === "undefined") return;
    localStorage.setItem(KEY, today());
  } catch {
    // localStorage 不可用時 silently noop
  }
}
```

- [ ] **Step 1.4：跑測試確認通過**

```bash
npm test -- daily-limit.test
```

預期：4 tests pass。

- [ ] **Step 1.5：commit**

```bash
git add apps/web-pwa/src/lib/daily-limit.ts apps/web-pwa/src/lib/__tests__/daily-limit.test.ts
git commit -m "feat(limit): localStorage daily-limit（軟限制 1/天）"
```

---

## Task 2：App.tsx 整合 daily-limit + UI 訊息

**Files:**
- Modify: `apps/web-pwa/src/App.tsx`
- Modify: `apps/web-pwa/src/styles.css`

- [ ] **Step 2.1：App.tsx import + state**

讀 `apps/web-pwa/src/App.tsx`，在既有 import 那段加：

```ts
import { hasUsedLetterToday, markLetterSent } from "./lib/daily-limit";
```

在 App component state 那段加（最好放 `useEffect(() => { setSaved(loadSaved()); }, [])` 附近）：

```ts
const [dailyLimitReached, setDailyLimitReached] = useState(false);

useEffect(() => {
  setDailyLimitReached(hasUsedLetterToday());
}, []);
```

- [ ] **Step 2.2：submit() 加軟限制 check + 成功後 mark**

找到 `async function submit()` 第一行加：

```ts
if (dailyLimitReached) return;
```

找到 setResponse(letter) **成功路徑**那行**之後**（在 setIsResponding(true) 前後都行），加：

```ts
markLetterSent();
setDailyLimitReached(true);
```

注意：crisis 路徑（不打 API 直接 buildResponse）也要 markLetterSent，因為使用者送了一次紙條。

- [ ] **Step 2.3：composer 變唯讀 + 顯示訊息**

找到 ventActive 那段 composer 渲染 JSX。在 composer textarea 之前加：

```tsx
{dailyLimitReached && (
  <p className="daily-limit-notice">
    今晚的紙條已經投了。明天燈還會亮著。
  </p>
)}
```

把 textarea 加 disabled：

```tsx
<textarea
  ref={textareaRef}
  value={input}
  onChange={(event) => setInput(event.target.value)}
  placeholder={ventPlaceholder}
  rows={3}
  aria-label="寫一張投進樹洞的紙條"
  disabled={dailyLimitReached}
/>
```

把 submit button 加 disabled：

```tsx
<button type="button" className="send-button" onClick={submit} aria-label="送進樹洞" disabled={dailyLimitReached}>
  投
</button>
```

- [ ] **Step 2.4：加 .daily-limit-notice 樣式**

附加到 `apps/web-pwa/src/styles.css` 結尾：

```css
.daily-limit-notice {
  margin: 0 0 12px;
  padding: 10px 14px;
  border-left: 2px solid rgba(155, 184, 116, 0.6);
  background: rgba(155, 184, 116, 0.08);
  color: var(--ink);
  font-family: "Noto Sans TC", "PingFang TC", system-ui, sans-serif;
  font-size: 0.88rem;
  line-height: 1.6;
  letter-spacing: 0.02em;
}
```

- [ ] **Step 2.5：驗證 build + tests**

```bash
npm run build
npm test
```

預期：build OK、46+ tests pass。

- [ ] **Step 2.6：commit**

```bash
git add apps/web-pwa/src/App.tsx apps/web-pwa/src/styles.css
git commit -m "feat(limit): submit() 整合 daily-limit + composer 唯讀提示"
```

---

## Task 3：rate-limit Vercel KV wrapper（TDD）

**Files:**
- Create: `api/_lib/rate-limit.ts`
- Create: `api/_lib/__tests__/rate-limit.test.ts`
- Modify: `package.json` (root) — 加 `@vercel/kv` 依賴

- [ ] **Step 3.1：安裝 SDK**

```bash
cd /Users/lcc/Desktop/PGY許願池/藥師樹洞
npm install --save @vercel/kv
```

預期：package.json 多一行 `"@vercel/kv": "^x.y.z"`。

- [ ] **Step 3.2：寫失敗測試**

`api/_lib/__tests__/rate-limit.test.ts`：

```ts
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

const { kvIncrMock, kvExpireMock } = vi.hoisted(() => ({
  kvIncrMock: vi.fn(),
  kvExpireMock: vi.fn()
}));

vi.mock("@vercel/kv", () => ({
  kv: {
    incr: kvIncrMock,
    expire: kvExpireMock
  }
}));

describe("checkAndIncrementIp", () => {
  beforeEach(() => {
    kvIncrMock.mockReset();
    kvExpireMock.mockReset();
  });

  afterEach(() => {
    vi.unstubAllEnvs();
  });

  it("第一次：counter incr → 1，allowed=true、count=1、limit=5", async () => {
    kvIncrMock.mockResolvedValue(1);
    const { checkAndIncrementIp } = await import("../rate-limit");
    const result = await checkAndIncrementIp("1.2.3.4");
    expect(result).toEqual({ allowed: true, count: 1, limit: 5 });
    expect(kvIncrMock).toHaveBeenCalledWith(expect.stringContaining("1.2.3.4"));
    expect(kvExpireMock).toHaveBeenCalled();
  });

  it("第 5 次：仍 allowed=true", async () => {
    kvIncrMock.mockResolvedValue(5);
    const { checkAndIncrementIp } = await import("../rate-limit");
    const result = await checkAndIncrementIp("1.2.3.4");
    expect(result.allowed).toBe(true);
    expect(result.count).toBe(5);
  });

  it("第 6 次：allowed=false", async () => {
    kvIncrMock.mockResolvedValue(6);
    const { checkAndIncrementIp } = await import("../rate-limit");
    const result = await checkAndIncrementIp("1.2.3.4");
    expect(result.allowed).toBe(false);
    expect(result.count).toBe(6);
  });

  it("KV 失敗時 fail-open（allowed=true 不擋）", async () => {
    kvIncrMock.mockRejectedValue(new Error("KV down"));
    const { checkAndIncrementIp } = await import("../rate-limit");
    const result = await checkAndIncrementIp("1.2.3.4");
    expect(result.allowed).toBe(true);
    expect(result.count).toBe(0);
  });
});
```

- [ ] **Step 3.3：跑測試確認失敗**

```bash
npm test -- rate-limit.test
```

預期：import 失敗。

- [ ] **Step 3.4：建立 `api/_lib/rate-limit.ts`**

```ts
import { kv } from "@vercel/kv";

const LIMIT = 5;
const TTL_SECONDS = 86400;

function today(): string {
  return new Date().toISOString().slice(0, 10);
}

function keyFor(ip: string): string {
  return `rate:${ip}:${today()}`;
}

export interface RateLimitResult {
  allowed: boolean;
  count: number;
  limit: number;
}

export async function checkAndIncrementIp(ip: string): Promise<RateLimitResult> {
  try {
    const key = keyFor(ip);
    const count = await kv.incr(key);
    if (count === 1) {
      await kv.expire(key, TTL_SECONDS);
    }
    return {
      allowed: count <= LIMIT,
      count,
      limit: LIMIT
    };
  } catch {
    return { allowed: true, count: 0, limit: LIMIT };
  }
}
```

- [ ] **Step 3.5：跑測試確認通過**

```bash
npm test -- rate-limit.test
```

預期：4 tests pass。

- [ ] **Step 3.6：commit**

```bash
git add api/_lib/rate-limit.ts api/_lib/__tests__/rate-limit.test.ts package.json package-lock.json
git commit -m "feat(api): IP rate-limit Vercel KV wrapper（5/IP/天、fail-open）"
```

---

## Task 4：budget cutoff（TDD）

**Files:**
- Create: `api/_lib/budget.ts`
- Create: `api/_lib/__tests__/budget.test.ts`

- [ ] **Step 4.1：寫失敗測試**

`api/_lib/__tests__/budget.test.ts`：

```ts
import { afterEach, describe, expect, it, vi } from "vitest";

describe("isBudgetExhausted", () => {
  afterEach(() => {
    vi.unstubAllEnvs();
  });

  it("沒設環境變數 → false（不限）", async () => {
    vi.stubEnv("BUDGET_HARD_CUTOFF_DATE", "");
    const { isBudgetExhausted } = await import("../budget");
    expect(isBudgetExhausted()).toBe(false);
  });

  it("cutoff 日期在未來 → false", async () => {
    vi.stubEnv("BUDGET_HARD_CUTOFF_DATE", "2099-12-31");
    const { isBudgetExhausted } = await import("../budget");
    expect(isBudgetExhausted()).toBe(false);
  });

  it("cutoff 日期在過去 → true（爆預算）", async () => {
    vi.stubEnv("BUDGET_HARD_CUTOFF_DATE", "2020-01-01");
    const { isBudgetExhausted } = await import("../budget");
    expect(isBudgetExhausted()).toBe(true);
  });
});
```

- [ ] **Step 4.2：跑測試確認失敗**

```bash
npm test -- budget.test
```

預期：import 失敗。

- [ ] **Step 4.3：建立 `api/_lib/budget.ts`**

```ts
export function isBudgetExhausted(): boolean {
  const cutoffRaw = process.env.BUDGET_HARD_CUTOFF_DATE;
  if (!cutoffRaw) return false;

  const cutoff = new Date(cutoffRaw);
  if (Number.isNaN(cutoff.getTime())) return false;

  return cutoff.getTime() < Date.now();
}
```

- [ ] **Step 4.4：跑測試確認通過**

```bash
npm test -- budget.test
```

預期：3 tests pass。

- [ ] **Step 4.5：commit**

```bash
git add api/_lib/budget.ts api/_lib/__tests__/budget.test.ts
git commit -m "feat(api): budget hard cutoff（env BUDGET_HARD_CUTOFF_DATE）"
```

---

## Task 5：/api/respond 整合 rate-limit + budget

**Files:**
- Modify: `api/respond.ts`
- Modify: `.env.example`

- [ ] **Step 5.1：讀現有 respond.ts**

```bash
cat api/respond.ts
```

確認結構：method check → body check → input/mood check → truncate → try callGemini → 200 / catch 502。

- [ ] **Step 5.2：在 method check 之後加 budget check**

找到 `if (req.method !== "POST")` 那段 block 之後，加：

```ts
import { isBudgetExhausted } from "./_lib/budget.js";

// （上面是新 import；以下放在 req.method check 之後）
if (isBudgetExhausted()) {
  res.status(503).json({ error: "budget_exhausted" });
  return;
}
```

- [ ] **Step 5.3：在 mood 驗證之後加 IP rate limit**

找到 `if (!VALID_MOODS.includes(mood))` 之後，加：

```ts
import { checkAndIncrementIp } from "./_lib/rate-limit.js";

// （上面是新 import；以下放在 mood check 之後、truncate 之前）
const ip = (req.headers["x-forwarded-for"] as string | undefined)?.split(",")[0]?.trim()
  ?? req.headers["x-real-ip"] as string | undefined
  ?? "unknown";
const limit = await checkAndIncrementIp(ip);
if (!limit.allowed) {
  res.status(429).json({ error: "rate_limit", count: limit.count, limit: limit.limit });
  return;
}
```

- [ ] **Step 5.4：完整 respond.ts 應該是**

最終 imports 區段：

```ts
import type { VercelRequest, VercelResponse } from '@vercel/node';
import { callGemini } from './_lib/gemini-client.js';
import { checkAndIncrementIp } from './_lib/rate-limit.js';
import { isBudgetExhausted } from './_lib/budget.js';
```

handler 流程：method check → budget check → body check → input check → mood check → IP rate limit check → truncate → callGemini。

- [ ] **Step 5.5：tsc 驗證**

```bash
npx tsc -p api/tsconfig.json --noEmit
```

預期：exit 0。

- [ ] **Step 5.6：更新 .env.example**

附加到結尾：

```bash
# Vercel KV（Storage → Create Database → KV，自動注入這 4 個）
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
KV_URL=

# 月預算 hard cutoff（optional）
# 設成 YYYY-MM-DD，超過該日期直接 fallback 不打 AI
BUDGET_HARD_CUTOFF_DATE=
```

- [ ] **Step 5.7：commit**

```bash
git add api/respond.ts .env.example
git commit -m "feat(api): respond handler 整合 IP rate-limit + budget cutoff"
```

---

## Task 6：letter-client 處理 429 + dailyLimit reset UX

**Files:**
- Modify: `apps/web-pwa/src/lib/letter-client.ts`
- Modify: `apps/web-pwa/src/lib/__tests__/letter-client.test.ts`

- [ ] **Step 6.1：letter-client 加 429 處理**

讀 `apps/web-pwa/src/lib/letter-client.ts`，現有 fallback 規則「response not ok」就 fallback。改成：**429 不 fallback，直接 throw** 一個特定 error，讓 App.tsx 知道是「rate limited」、不要 fallback 假裝有 AI。

修改 `requestAILetter`：

```ts
export class RateLimitedError extends Error {
  constructor() {
    super("rate_limit");
    this.name = "RateLimitedError";
  }
}

export async function requestAILetter(input: string, mood: MoodTag): Promise<LetterResult> {
  try {
    const response = await fetch("/api/respond", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ input, mood })
    });

    if (response.status === 429) {
      throw new RateLimitedError();
    }

    if (!response.ok) return fallback(input, mood);

    const data = await response.json();
    if (!looksLikeAILetter(data)) return fallback(input, mood);

    return { letter: data, source: "ai" };
  } catch (err) {
    if (err instanceof RateLimitedError) throw err;
    return fallback(input, mood);
  }
}
```

- [ ] **Step 6.2：加新測試 — 429 throws RateLimitedError**

加到 `apps/web-pwa/src/lib/__tests__/letter-client.test.ts` describe 內：

```ts
it("API 429 → throws RateLimitedError（不 fallback）", async () => {
  global.fetch = vi.fn().mockResolvedValue({
    ok: false,
    status: 429,
    json: async () => ({ error: "rate_limit" })
  }) as unknown as typeof fetch;

  const { requestAILetter, RateLimitedError } = await import("../letter-client");
  await expect(requestAILetter("test", "煩")).rejects.toBeInstanceOf(RateLimitedError);
  expect(buildResponseMock).not.toHaveBeenCalled();
});
```

- [ ] **Step 6.3：跑測試**

```bash
npm test -- letter-client.test
```

預期：6 tests pass。

- [ ] **Step 6.4：App.tsx submit() 接 RateLimitedError**

讀 `apps/web-pwa/src/App.tsx`，import 加：

```ts
import { requestAILetter, RateLimitedError } from "./lib/letter-client";
```

submit() 內 `await requestAILetter(safeText, mood)` 那段包 try/catch：

```ts
} else {
  const safeText = redactSensitiveText(userText);
  try {
    const result = await requestAILetter(safeText, mood);
    letter = result.letter;
  } catch (err) {
    if (err instanceof RateLimitedError) {
      setIsThinking(false);
      setRateLimitHit(true);
      return;
    }
    throw err;
  }
}
```

加 state：

```ts
const [rateLimitHit, setRateLimitHit] = useState(false);
```

加 UI（在 composer 上方、和 dailyLimitNotice 並列）：

```tsx
{rateLimitHit && (
  <p className="daily-limit-notice">
    今晚這裡比較滿，明天再試試。
  </p>
)}
```

- [ ] **Step 6.5：驗證 build + tests**

```bash
npm run build
npm test
```

預期：build OK、47+ tests pass（原 46 + daily-limit 4 + rate-limit 4 + budget 3 + 新 429 test 1 = 58）。

- [ ] **Step 6.6：commit**

```bash
git add apps/web-pwa/src/lib/letter-client.ts apps/web-pwa/src/lib/__tests__/letter-client.test.ts apps/web-pwa/src/App.tsx
git commit -m "feat(limit): 前端接 429 → RateLimitedError + 「這裡比較滿」訊息"
```

---

## Task 7：揭露文案精修

**Files:**
- Modify: `apps/web-pwa/src/components/ResponseCard.tsx`
- Modify: `apps/web-pwa/src/styles.css`

評估：目前揭露在每封信底部、ink-soft 0.74rem opacity 0.68。每次都看到一次，可能變視覺噪音。

**改成只在首次顯示**（用 localStorage 記錄）：

- [ ] **Step 7.1：建立 disclosure-seen helper**

`apps/web-pwa/src/lib/disclosure.ts`：

```ts
const KEY = "pharmacist-tree-hollow:disclosureSeen";

export function hasDisclosureSeen(): boolean {
  try {
    return typeof localStorage !== "undefined" && localStorage.getItem(KEY) === "1";
  } catch {
    return false;
  }
}

export function markDisclosureSeen(): void {
  try {
    if (typeof localStorage !== "undefined") {
      localStorage.setItem(KEY, "1");
    }
  } catch {
    // noop
  }
}
```

對應測試 `apps/web-pwa/src/lib/__tests__/disclosure.test.ts`：

```ts
import { afterEach, beforeEach, describe, expect, it } from "vitest";
import { hasDisclosureSeen, markDisclosureSeen } from "../disclosure";

describe("disclosure", () => {
  beforeEach(() => { localStorage.clear(); });
  afterEach(() => { localStorage.clear(); });

  it("第一次 hasDisclosureSeen 回 false", () => {
    expect(hasDisclosureSeen()).toBe(false);
  });

  it("markDisclosureSeen 後 hasDisclosureSeen 回 true", () => {
    markDisclosureSeen();
    expect(hasDisclosureSeen()).toBe(true);
  });
});
```

- [ ] **Step 7.2：ResponseCard 條件顯示揭露**

讀 `apps/web-pwa/src/components/ResponseCard.tsx`，import：

```ts
import { hasDisclosureSeen, markDisclosureSeen } from "../lib/disclosure";
```

加 state：

```ts
const [showDisclosure] = useState(() => !hasDisclosureSeen());

useEffect(() => {
  if (showDisclosure) markDisclosureSeen();
}, [showDisclosure]);
```

把 `<p className="letter-disclosure">...</p>` 包：

```tsx
{showDisclosure && (
  <p className="letter-disclosure">樹洞回信由 AI 協助撰寫，內容不會被保留訓練。</p>
)}
```

- [ ] **Step 7.3：ResponseCard.test 加新測試**

把現有 test「顯示 AI 揭露文案」加註：第一次顯示。新加：

```ts
it("第二封信不再顯示揭露文案（已看過）", () => {
  localStorage.setItem("pharmacist-tree-hollow:disclosureSeen", "1");
  const html = renderToStaticMarkup(
    <ResponseCard letter={aiLetter} onSave={() => undefined} onNewNote={() => undefined} />
  );
  expect(html).not.toContain("樹洞回信由 AI 協助撰寫");
});
```

修改第一個揭露 test 加 `localStorage.clear()` beforeEach 或顯式 setup。

- [ ] **Step 7.4：跑測試**

```bash
npm test -- ResponseCard.test disclosure.test
```

預期：6+ tests pass。

- [ ] **Step 7.5：commit**

```bash
git add apps/web-pwa/src/lib/disclosure.ts apps/web-pwa/src/lib/__tests__/disclosure.test.ts apps/web-pwa/src/components/ResponseCard.tsx apps/web-pwa/src/components/ResponseCard.test.tsx
git commit -m "feat(letter): 揭露文案只在首次信件顯示（localStorage 記憶）"
```

---

## Task 8：本地 e2e + push

- [ ] **Step 8.1：跑所有測試**

```bash
npm test
```

預期：全綠（總計 56+ tests）。

- [ ] **Step 8.2：跑 build**

```bash
npm run build
```

預期：build OK。

- [ ] **Step 8.3：dev 驗收 daily-limit（軟限制）**

```bash
npm run dev
```

開無痕分頁 → 寫紙條 → 投 → 收到信。

清 composer 想再投 → 應該看到「今晚的紙條已經投了。明天燈還會亮著。」 + textarea / 投按鈕 disabled。

打開 DevTools Application → localStorage → 把 `pharmacist-tree-hollow:lastLetterDate` 改成昨天日期 → 重整 → 應該又可以投。

- [ ] **Step 8.4：dev 驗收揭露（只首次）**

清 localStorage → 投紙條 → 看到揭露文字。

再投一次 → 應該不再有揭露文字。

- [ ] **Step 8.5：dev 驗收 rate-limit（後端 429）**

⚠️ dev 沒 /api/respond、走 fallback、所以 429 路徑無法本地測。

跳到 Step 8.7 production 驗。

- [ ] **Step 8.6：commit 沒提交的微調（如有）**

```bash
git status
# 沒事跳過
```

- [ ] **Step 8.7：push 並驗收 production**

```bash
git push origin main
```

等 Vercel 部署完（~40s）後測：

```bash
# 第 1-5 次應該成功
for i in 1 2 3 4 5; do
  echo "--- attempt $i ---"
  curl -s -X POST https://pharmacist-tree-hollow.vercel.app/api/respond \
    -H "Content-Type: application/json" \
    -d '{"input":"今天又被罵","mood":"煩"}' \
    -i | head -1
done

# 第 6 次應該 429
curl -s -X POST https://pharmacist-tree-hollow.vercel.app/api/respond \
  -H "Content-Type: application/json" \
  -d '{"input":"今天又被罵","mood":"煩"}' \
  -i | head -3
```

預期：頭 5 次 HTTP/2 200、第 6 次 HTTP/2 429 + `{"error":"rate_limit",...}`。

切片 C 完成。

---

## 預期障礙與緩解

| 障礙 | 症狀 | 緩解 |
|---|---|---|
| Vercel KV 尚未在 dashboard 建立 | rate-limit 測試本地 pass、production /api/respond 500 | Task 0 先做、把 KV 連到 project、env vars 自動注入 |
| 本地 dev 沒 KV 連線 | rate-limit fail-open（allowed=true）→ dev 不擋 | 預期；KV mock 在 unit tests 蓋了、production 才實測 |
| 429 出現後 UI 卡死 | dailyLimit 沒被 reset、再投不了 | 不算 bug；rate limit 觸發本就應該擋 |
| 揭露 only-first 導致使用者忘記 AI | 已看過的使用者再回來看不到 | 加 about / settings 頁的「關於」連結（不在切片 C 範圍、未來 v0.6 處理） |
| IP 偵測 spoofing | `x-forwarded-for` 可被偽造 | Vercel 已過濾、產品 niche 不擔心職業 spammer |
| 多藥師共用同一 IP（醫院/藥局 wifi）| 5 個藥師一起被限 | 5/天/IP buffer 還算 OK；極端情況下使用者吃 429 |
| budget cutoff 設錯日期格式 | 沒擋下 | budget.ts 用 new Date() + NaN 判斷、解析失敗回 false（fail-open） |

---

## 本地驗收清單（DoD）

切片 C 完成 ⇔ 以下都通過：

- [ ] daily-limit 模組 + 4 tests 全綠
- [ ] rate-limit 模組 + 4 tests 全綠
- [ ] budget 模組 + 3 tests 全綠
- [ ] disclosure 模組 + 2 tests 全綠
- [ ] letter-client 429 test pass（總 6 tests）
- [ ] App.tsx submit() 加 dailyLimit + rateLimitHit
- [ ] composer 在 dailyLimit 時 disabled + 顯示「今晚的紙條已經投了」
- [ ] rate-limit 觸發 429 時顯示「今晚這裡比較滿」
- [ ] ResponseCard 揭露文案只在首次顯示
- [ ] `npm run build` 成功
- [ ] `npm test` 全綠
- [ ] Vercel KV store 已建立並連到 project
- [ ] env vars 在 Vercel production 已自動注入
- [ ] production curl 第 6 次回 429

---

## 切片 C 完成後下一步

進入 v0.5 polish 切片 ② 或 ③：
- **切片 ②：小屋音樂 A/B test** — Spotify search URL vs Embed iframe、localStorage 50/50 分流
- **切片 ③：LOFI BGM** — `<audio loop>` + 右下角靜音 toggle + Pixabay CC0 音檔

使用者已指定下一步是「小屋音樂」、推進這條。
